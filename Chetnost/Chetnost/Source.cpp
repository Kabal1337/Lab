#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
#include<cmath>
#include<windows.h>
using namespace std;
inline void keep_window_open() { char ch; cin >> ch; }

int main()
{
	SetConsoleOutputCP(1251);
	SetConsoleCP(1251);
	setlocale(0, "");
	int chislo{ 0 };
	vector<double> chisla;
	int i{ 0 };
	for (double chislo; cin >> chislo; ++i) {
		chisla.push_back(chislo);
		for (int a; chislo<chisla[a];++a)

	}
		
		
		




}